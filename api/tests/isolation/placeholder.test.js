import { describe, expect, it } from 'vitest';

describe('isolation suite wiring', () => {
  it('is wired and will be replaced by real cross-company leak tests in Task 2/10', () => {
    expect(true).toBe(true);
  });
});
